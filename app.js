// node used for
// 1.web servers, native apps(e.g: vs code), video games

// js vs Node.js
// document and window are not run in node
// there are not support dom,document.
//but node.js have a build in modules to interact with the os and files and folders.

// currently working path
// Process.cwd()

// file system

// module.exports
// const add = (x,y) => (x+y);
// const squ = x => x*x;
// module.exports.squ = squ;
// module.exports.add = add;

// npm

// types of modules
// 1.localmodule,2.coremodule,3.thirdpartymodule

// coremodule
// const readline=require('readline');

// const rl = readline.createInterface({
//     input:process.stdin,
//     output:process.stdout
// });

// rl.question('what' +"'"+ 's' + ' your age? ',(ans)=>{
//     console.log("my age is " + ans);
//     rl.close();
// })

//REST API ->http->get,post,patch,delete
